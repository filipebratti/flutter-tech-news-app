// Centralize app colors here
import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1565C0);
  static const accent = Color(0xFF00BCD4);
  static const background = Colors.white;
  // Add more colors as needed
}
